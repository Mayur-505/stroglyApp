import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Brand Accent Color from Figma
  static const Color primaryLime = Color(0xFFA3D223);

  // Background Tone from Figma: rgba(0, 0, 0, 1)
  static const Color backgroundBlack = Color(0xFF000000);

  // Text Colors
  static const Color textWhite = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xCCFFFFFF);

  // Gradient Stops from Figma:
  // linear-gradient(180deg, rgba(163, 210, 35, 0) 0%, #A3D223 100%)
  static const Color gradientStart = Color(0x00A3D223);
  static const Color gradientEnd = Color(0xFFA3D223);
}
